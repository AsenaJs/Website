{
  "dependencies": {},
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "bun run docs:llms && vitepress build",
    "docs:llms": "bun scripts/generate-llms.ts",
    "docs:preview": "vitepress preview"
  }
}